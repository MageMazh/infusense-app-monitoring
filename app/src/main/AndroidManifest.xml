<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Notifikasi (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Foreground service permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Spesifik tipe untuk Android 14+ (tidak masalah untuk versi lama) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/img_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.InfuSense"
        tools:targetApi="31">

        <!-- Splash / Main / Edit -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false" />

        <activity
            android:name=".ui.edittreshold.EditThresholdActivity"
            android:exported="false" />

        <!-- MonitoringService: jalan TERUS (foreground ringan) -->
<!--        <service-->
<!--            android:name=".service.InfusMessagingService"-->
<!--            android:exported="false"-->
<!--            android:foregroundServiceType="dataSync" />-->

<!--        <service-->
<!--            android:name=".notification.MonitorService"-->
<!--            android:exported="false"-->
<!--            android:foregroundServiceType="dataSync" />-->

<!--        &lt;!&ndash; AlarmService: saat bunyi alarm &ndash;&gt;-->
<!--        <service-->
<!--            android:name=".notification.AlarmService"-->
<!--            android:exported="false"-->
<!--            android:foregroundServiceType="mediaPlayback" />-->

        <service
            android:name=".service.MonitorService"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:stopWithTask="true" />

        <service
            android:name=".notification.AlarmService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"
            android:stopWithTask="true" />

    </application>
</manifest>
